---
/**
 * Sección: Flujo de Uso Simplificado (3 pasos)
 * - Sin React: render estático y liviano
 * - Lee textos desde site.json (patrón import * as siteData)
 */
import * as siteData from "../../site.json";
const data = siteData.flow;
---

<section id={data.id} class="relative">
  <div class="mx-auto max-w-7xl px-6 py-16 sm:py-20">
    <!-- Títulos -->
    <div class="text-center mb-10">
      <h2 class="text-2xl sm:text-4xl font-extrabold tracking-tight text-slate-900">
        {data.title}
      </h2>
      <p class="mt-3 text-slate-500 max-w-3xl mx-auto">
        {data.subtitle}
      </p>
    </div>

    <!-- Pasos -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
      {data.steps.map((step) => (
        <div class="text-center">
          <!-- Badge circular con glow -->
          <div class="mx-auto mb-6 flex h-24 w-24 items-center justify-center rounded-full bg-gradient-to-br from-[#059275] via-[#01a182] to-[#4646a7] text-white shadow-xl ring-4 ring-sky-100">
            <span class="text-2xl font-extrabold">{step.num}</span>
          </div>

          <!-- Título -->
          <h3 class="text-lg sm:text-xl font-extrabold text-slate-900">
            {step.title}
          </h3>

          <!-- Descripción -->
          <p class="mt-2 text-sm text-slate-600 max-w-sm mx-auto leading-relaxed">
            {step.desc}
          </p>

          <!-- Icono -->
          <div class="mt-5 flex justify-center">
            {
              step.icon === "device" && (
                <svg class="h-10 w-10 text-sky-600" viewBox="0 0 24 24" fill="none" stroke="#059275" stroke-width="1.8">
                  <rect x="7" y="3" width="10" height="18" rx="2" />
                  <circle cx="12" cy="18" r="1.2" fill="currentColor" stroke="none" />
                </svg>
              )
            }
            {
              step.icon === "bolt" && (
                <svg class="h-10 w-10 text-sky-600" viewBox="0 0 24 24" fill="#059275">
                  <path d="M13 2 3 14h7l-1 8 11-14h-7l0-6z" />
                </svg>
              )
            }
            {
              step.icon === "fingerprint" && (
                <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#059275" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-fingerprint-icon lucide-fingerprint"><path d="M12 10a2 2 0 0 0-2 2c0 1.02-.1 2.51-.26 4"/><path d="M14 13.12c0 2.38 0 6.38-1 8.88"/><path d="M17.29 21.02c.12-.6.43-2.3.5-3.02"/><path d="M2 12a10 10 0 0 1 18-6"/><path d="M2 16h.01"/><path d="M21.8 16c.2-2 .131-5.354 0-6"/><path d="M5 19.5C5.5 18 6 15 6 12a6 6 0 0 1 .34-2"/><path d="M8.65 22c.21-.66.45-1.32.57-2"/><path d="M9 6.8a6 6 0 0 1 9 5.2v2"/></svg>
              )
            }
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
